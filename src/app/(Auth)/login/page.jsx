import LoginForm from "@/components/LoginForm";

export const metadata = {
  title: "Available Rooms", // This string replaces the "%s" placeholder automatically!
};

export default function LoginPage() {
  return (
    <>
      <LoginForm />
    </>
  );
}
